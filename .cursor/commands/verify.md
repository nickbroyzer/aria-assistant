# /verify — Run QA Verification Gate

1. Check Flask server is running on port 5001
2. Curl all API endpoints and validate JSON responses
3. Extract and syntax-check all script blocks from dashboard.html
4. Report results in the QA verification format
5. Do NOT allow any git commit until all checks pass
