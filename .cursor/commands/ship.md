# /ship — Commit and deploy

1. Run full /verify first — stop if any check fails
2. Stage all changes: git add -A
3. Write a descriptive commit message based on what changed
4. git commit
5. git push origin main
6. Confirm Railway auto-deploy will pick it up
